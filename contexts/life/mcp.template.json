{
  "_comment": "生活情境 MCP 白名單 — 只列個人工具，複製為 mcp.json 並填入真實設定",
  "mcpServers": {
    "google-drive-personal": {
      "command": "mcp-google-drive",
      "args": [],
      "env": {
        "GOOGLE_CREDENTIALS": "${GOOGLE_PERSONAL_CREDENTIALS}"
      }
    },
    "google-calendar": {
      "command": "mcp-google-calendar",
      "args": [],
      "env": {
        "GOOGLE_CREDENTIALS": "${GOOGLE_PERSONAL_CREDENTIALS}"
      }
    }
  }
}
