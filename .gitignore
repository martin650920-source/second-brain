# ═══════════════════════════════════════════════════════
# ai-workspace .gitignore
# 策略：不做公私分離，所有 workspace 內容（rules/mem/projects/skills 等）
# 一律進 Git，作為多主機/多平台共用的單一事實來源。
# 唯一例外是「真實憑證」（見下方機密區塊）——那是安全衛生問題，不是本策略的一部分。
# ═══════════════════════════════════════════════════════

# ── 工作專案 / 個人規則、筆記 ──────────────────────────
# （2026-07-02 移除白名單限制，2026-07-03 進一步移除 rules/global.local.md
#  的排除規則，不再區分公開骨架/私密內容，全部進 git 版控。
#  依 ai-workspace-design.md 原始設計：單一事實來源 + symlink 多主機同步。
#  風險自負：repo 目前為 Public，內容機密性由使用者自行把關。）

# ── 機密（API keys / tokens）─────────────────────────
# 真實憑證放使用者家目錄 ~/.mcp.env，不進這個 repo，
# 這裡只保留 mcp/.env.example 範本（見下方 !）

# ── 知識庫（第二階段，含個人知識）───────────────────
/wiki/
/raw/

# ── 系統 / 工具 ─────────────────────────────────────
.DS_Store
*.swp
*~
.obsidian/
Thumbs.db
*.stackdump
